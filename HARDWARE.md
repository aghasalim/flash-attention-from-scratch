# HARDWARE

Generated by `python -m scripts.env` at **2026-08-24 10:18:24 CEST**. Every number below was measured by that script on this machine or read from a real device query. Nothing is copied from a spec sheet, and nothing is extrapolated to a GPU I do not own.

> **No CUDA device.** Built on an Apple M4 (arm64, macOS). torch.cuda.is_available() is False and Triton publishes no macOS wheel, so every NVIDIA-only field in this file is null and renders in HARDWARE.md as: 'not measured on this hardware (no CUDA device; developed on Apple M4)'. The mps and cpu numbers here are real measurements taken by scripts/env.py on this machine; they are Apple GPU / CPU numbers and are never a stand-in for a CUDA result. Re-run with: python -m scripts.env

## Summary

| field | value |
|---|---|
| device_class | apple-mps |
| cuda_available | False |
| has_mps | True |
| name | Apple M4 |
| gpu backend | Apple Metal via torch MPS |
| GPU cores / SM count | 10 |
| CPU cores (logical) | 10 |
| CPU perf / efficiency cores | 4 / 6 |
| memory | 25.77 GB (25769803776 bytes) |
| memory model | unified (CPU and GPU share physical memory) |

## NVIDIA device fields

| field | value | why |
|---|---|---|
| compute_capability | not measured on this hardware (no CUDA device; developed on Apple M4) | NVIDIA SM version; nothing to query without a CUDA device |
| sm_count | not measured on this hardware (no CUDA device; developed on Apple M4) | streaming multiprocessors are an NVIDIA concept |
| total_memory_bytes | not measured on this hardware (no CUDA device; developed on Apple M4) | dedicated HBM/GDDR; this machine has unified memory instead (recorded above) |
| shared_memory_per_sm_bytes | not measured on this hardware (no CUDA device; developed on Apple M4) | CUDA shared memory; Metal threadgroup memory is a different thing and is not queried here |
| shared_memory_per_block_bytes | not measured on this hardware (no CUDA device; developed on Apple M4) | same; CUDA-only device attribute |
| regs_per_sm | not measured on this hardware (no CUDA device; developed on Apple M4) | CUDA register file per SM; no equivalent query on Metal |
| max_threads_per_block | not measured on this hardware (no CUDA device; developed on Apple M4) | CUDA launch geometry limit |
| warp_size | not measured on this hardware (no CUDA device; developed on Apple M4) | CUDA warp; Metal has 32-wide SIMD groups but that is not the same attribute and is not queried here |
| l2_cache_bytes | not measured on this hardware (no CUDA device; developed on Apple M4) | reported by cudaDeviceProp only |

## Measured memory bandwidth

| device | achieved | how |
|---|---|---|
| mps | **95.5 GB/s median [89.6-95.9]** | `x.copy_(y)` fp16, 1 GiB per buffer, 2 GiB moved per iter (read+write), 10 warmup + median of 20 timed runs, device-synced each run |
| cpu | **94.0 GB/s median [82.5-99.3]** | `x.copy_(y)` fp16, 1 GiB per buffer, 2 GiB moved per iter (read+write), 10 warmup + median of 20 timed runs, device-synced each run |
| cuda | **not measured on this hardware (no CUDA device; developed on Apple M4)** | no CUDA device to run a copy kernel on |

## Measured matmul throughput

FLOPs = `2*M*N*K`, square matrices. Median over the timed runs with the full min-max
range beside it. This is a fanless laptop: the same matmul has measured 25% apart
across two back-to-back runs depending on how warm the machine already was, so read
the range, not the median, as the honest statement of what this hardware does.

These are **Apple GPU (MPS) and CPU** numbers. They are not tensor-core numbers and
they are not a stand-in for any CUDA measurement.

| dtype | device | GFLOP/s | detail |
|---|---|---|---|
| fp16 | mps | **3142.6 GFLOP/s median [1791.2-3599.5]** | M=N=K=8192, median 349.9 ms, 10 warmup + 20 timed, torch.matmul default accumulation for this dtype/backend |
| fp16 | cpu | **3.3 GFLOP/s median [3.1-3.4]** | M=N=K=1024 (not 8192: one matmul there exceeds the 1.5s per-iteration budget on this device/dtype), median 644.9 ms, 10 warmup + 20 timed, torch.matmul default accumulation for this dtype/backend |
| bf16 | mps | **2744.8 GFLOP/s median [2570.2-2889.9]** | M=N=K=8192, median 400.6 ms, 10 warmup + 20 timed, torch.matmul default accumulation for this dtype/backend |
| bf16 | cpu | **3.3 GFLOP/s median [3.2-3.3]** | M=N=K=1024 (not 8192: one matmul there exceeds the 1.5s per-iteration budget on this device/dtype), median 655.5 ms, 10 warmup + 20 timed, torch.matmul default accumulation for this dtype/backend |
| fp32 | mps | **2331.8 GFLOP/s median [1817.4-2805.8]** | M=N=K=8192, median 471.5 ms, 10 warmup + 20 timed, torch.matmul default accumulation for this dtype/backend |
| fp32 | cpu | **1652.2 GFLOP/s median [1362.3-1744.2]** | M=N=K=8192, median 665.5 ms, 10 warmup + 20 timed, torch.matmul default accumulation for this dtype/backend |
| fp64 | mps | **dtype unsupported here** | TypeError: Cannot convert a MPS Tensor to float64 dtype as the MPS framework doesn't support float64. Please use float32 instead. |
| fp64 | cpu | **468.7 GFLOP/s median [364.5-470.7]** | M=N=K=4096 (not 8192: one matmul there exceeds the 1.5s per-iteration budget on this device/dtype), median 293.2 ms, 10 warmup + 20 timed, torch.matmul default accumulation for this dtype/backend |
| fp16/bf16/tf32 tensor core | cuda | not measured on this hardware (no CUDA device; developed on Apple M4) | there are no tensor cores on this machine |

## Capability flags

| flag | value | why |
|---|---|---|
| `has_bf16` | True | probed: 64x64 bf16 matmul on device='mps' |
| `has_cp_async` | False | CUDA sm_80+ PTX instruction; no CUDA device |
| `has_fp8` | False | CUDA sm_89+ tensor-core format; no CUDA device |
| `has_tma` | False | CUDA sm_90+ feature; no CUDA device |
| `has_wgmma` | False | CUDA sm_90+ feature; no CUDA device |
| `has_triton` | False | import triton -> not installed (no macOS wheels; see the [gpu] extra in pyproject.toml) |
| `fp64_on_mps` | False | probed: MPS has no float64, so fp64 references must run on the CPU |

## Versions

| what | version |
|---|---|
| python | 3.14.4 |
| torch | 2.13.0 |
| triton | not installed (no macOS wheels; see the [gpu] extra in pyproject.toml) |
| cuda_runtime | not measured on this hardware (no CUDA device; developed on Apple M4) |
| cudnn | not measured on this hardware (no CUDA device; developed on Apple M4) |
| driver | not measured on this hardware (no CUDA device; developed on Apple M4) |
| macos | 26.5.2 25F84 |

## Profiling

| tool | status |
|---|---|
| ncu | not measured on this hardware (no CUDA device; developed on Apple M4) |
| ncu_note | Nsight Compute is NVIDIA-only and is not installed here; `make profile` says so instead of pretending |
| alternative | Metal System Trace / Xcode Instruments (not wired up by task 00) |

## Platform

| field | value |
|---|---|
| system | Darwin |
| release | 25.5.0 |
| machine | arm64 |
| platform | macOS-26.5.2-arm64-arm-64bit-Mach-O |
| processor | Apple M4 |

## What this means for the rest of the project

- No NVIDIA GPU here: no Triton (no macOS wheel), no CUDA C++, no `ncu`. Tasks 03
  and 05-10 are written against CUDA and need a rented GPU before they can run.
- `fp64` does not exist on MPS, so every fp64 reference in `fa/ref/` runs on the CPU.
- Memory is unified: there is no host-to-device copy to hide, and CPU and GPU contend
  for the same bandwidth. A roofline drawn here is not a roofline for an A100.
- The CUDA ceiling for this project is unknown. It stays unknown until the kernels
  run on a real NVIDIA card, and no number in this repo will pretend otherwise.
